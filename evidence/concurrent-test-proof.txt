KẾT QUẢ KIỂM THỬ ĐA LUỒNG

- Số request đồng thời: 50
- ID được gọi: 1
- Tất cả request: thành công
- Số lần method thực sự truy xuất database: 1
- Log truy xuất database chỉ xuất hiện một lần:

  Fetching from Database for product 1

- Kết quả lần chạy thực tế: `50/50 request thành công`, `DB = 1`, `2064 ms`.
- Tổng thời gian xấp xỉ 2 giây cộng overhead, không phải 50 x 2 giây.

Chạy lại bằng lệnh:

  ./gradlew clean test --info

Test tự động sẽ thất bại nếu số lần truy xuất DB khác 1, có request lỗi hoặc
tổng thời gian vượt quá 4 giây.
